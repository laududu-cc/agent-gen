# AgentForge — 产品需求文档 v2.0

> 智能体生成平台：用户通过自然语言描述需求，自动生成对应类型的 AI 智能体，并支持在线试用与对话式反馈迭代。

---

## 1. 产品概述

### 1.1 产品定位

一个面向非技术用户的 Web 应用。用户只需用自然语言描述需求，系统自动生成对应类型的 AI 智能体（Agent），并支持在线即时测试与反馈调优。

### 1.2 核心设计理念

| 原则 | 说明 |
|---|---|
| **LLM-First** | 所有技术决策由大模型自主完成：类型判断、提示词生成、工具选择 |
| **零技术参与** | 用户不接触任何技术概念，只需说人话 |
| **最小交互量** | 创建智能体 ≤ 3 轮对话，反馈调优一句话完成 |
| **即时反馈** | 每次调整后立即可以测试，无需刷新或重建 |

### 1.3 目标用户

- 非技术业务人员，希望快速搭建专用 AI 助手
- 对 AI Agent 有需求但缺乏技术实现能力的人群

### 1.4 智能体类型（v2.0）

| 类型 | 说明 | 典型场景 |
|---|---|---|
| **普通智能体** | 纯对话型，基于提示词回答问题 | 写作助手、翻译工具、学习导师 |
| **工具调用智能体** | 可调用内置功能性 API 执行动作 | 查新闻、查天气、搜索信息 |

两种智能体的核心区别只有一个：**是否能调用工具动作**。底层都是 LLM + 提示词驱动。

---

## 2. 智能体类型详解

### 2.1 普通智能体

**工作原理**
```
用户用自然语言描述想要的助手
        ↓
LLM 生成 system prompt
        ↓
用户与智能体自由对话
        ↓
不满意 → 自然语言反馈 → LLM 重写 prompt → 继续对话
```

**特点**
- 纯对话交互，无外部知识注入
- 完全由 system prompt 定义行为和风格
- 适合通用对话、内容生成、知识问答等场景

### 2.2 工具调用智能体

**工作原理**
```
用户用自然语言描述想要的助手
        ↓
LLM 生成 system prompt + 推断需要哪些工具
        ↓
用户发送消息 → LLM 判断是否需要调用工具
        ├─ 需要 → 调用内置 API → 结果返回 LLM → 生成回答
        └─ 不需要 → 直接回答
        ↓
不满意 → 自然语言反馈 → LLM 重写 prompt/工具配置 → 继续测试
```

**特点**
- 继承普通智能体的全部能力
- 额外具备调用内置功能性 API 的能力
- LLM 自主判断何时调用、调用哪个、传什么参数
- 用户无需了解 API 细节，也无需配置任何参数

**工具调用示例**
```
用户: "帮我查一下今天北京的天气"
智能体: (调用 fetch_weather) "北京今天晴，28°C..."
用户: "再搜一下最近 AI 方面的新闻"
智能体: (调用 fetch_news) "以下是最近的 AI 新闻..."
```

---

## 3. 用户旅程

### 3.1 创建智能体

```
Step 1: 用户输入自然语言需求
        ↓
Step 2: LLM 判断信息是否充足
        ├─ 充足 → 直接生成
        └─ 不足 → LLM 动态反问（1-3 轮）
                    ↓
Step 3: LLM 完成内部决策
        ├─ 推断智能体类型（普通 / 工具调用）
        ├─ 生成 system prompt
        └─ 推断需要的工具（工具调用类）
                    ↓
Step 4: 创建成功 → 跳转到测试页
```

#### 创建对话示例

**场景一：普通智能体**

```
用户: "我想创建一个帮我写邮件的助手"
系统: "邮件风格偏向正式还是随意？主要写什么类型的邮件？"
用户: "正式的商务邮件"
系统: "✅ 已创建「商务邮件助手」（普通型），进入测试..."
```

**场景二：工具调用智能体**

```
用户: "创建一个能帮我查新闻和天气的助手"
系统: "除了查新闻和天气，还需要其他功能吗？比如查股价或搜索网页"
用户: "暂时不需要"
系统: "✅ 已创建「信息查询助手」（工具调用型），配备新闻查询、天气查询工具，进入测试..."
```

### 3.2 测试智能体

**通用测试页面**（两种类型共用同一界面）

```
┌────────────────────────────────────────────┐
│  ← 返回  |  商务邮件助手       [普通型]    │
│                                            │
│  ┌────────────────────────────────────┐   │
│  │  帮我写一封催款邮件               │   │
│  │  好的，以下是一封正式的催款邮件： │   │
│  │  尊敬的 XXX...                    │   │
│  │                                    │   │
│  │  [语气太强硬了，温和一点]        │   │
│  │  → 已调整，请再看：              │   │
│  │  尊敬的 XXX，您好...              │   │
│  └────────────────────────────────────┘   │
│  ┌──────────────────────────┐             │
│  │ 输入消息...                │             │
│  └──────────────────────────┘             │
└────────────────────────────────────────────┘
```

**工具调用型额外展示执行日志**

```
┌────────────────────────────────────────────┐
│  ← 返回  |  信息查询助手    [工具调用型]   │
│  可用工具: 新闻查询 · 天气查询 · 网页搜索  │
│                                            │
│  ┌────────────────────────────────────┐   │
│  北京天气怎么样？                     │   │
│  ┌ ─ ─ 调用工具: fetch_weather ─ ─ ┐ │   │
│  │  参数: {"city": "北京"}         │ │   │
│  │  → 北京今天晴，28-32°C...      │ │   │
│  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘ │   │
│  北京今天晴，28-32°C，南风3级...    │   │
│                                      │   │
│  [帮我查一下最近的AI新闻]           │   │
│  ┌ ─ ─ 调用工具: fetch_news ─ ─ ─ ┐ │   │
│  │  参数: {"keywords":["AI"]}     │ │   │
│  │  → 找到5条结果...              │ │   │
│  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘ │   │
│  以下是最近的 AI 新闻：...         │   │
│  └────────────────────────────────────┘   │
│  ┌──────────────────────────┐             │
│  │ 输入消息...                │             │
│  └──────────────────────────┘             │
└────────────────────────────────────────────┘
```

### 3.3 反馈调优

用户在测试过程中可随时用自然语言表达意见，系统自动调优。

| 用户反馈 | 系统行为 |
|---|---|
| "语气太正式了，口语化一些" | 重写 system prompt |
| "回答太长了，简短点" | 重写 system prompt，加入简洁指令 |
| "加上来源链接" | 重写 system prompt |
| "再帮我加一个查天气的功能" | 更新工具列表（工具调用型） |
| "很好，就这样" | 标记当前配置为满意状态 |

调优即时生效，用户在同一对话中继续测试，无需离开当前页面。

---

## 4. 功能性 API（工具调用智能体专用）

系统内置一组预设功能性 API，供工具调用智能体在对话中由 LLM 自主选择调用。

| API 名称 | 用途 | 参数（LLM 自动填充） |
|---|---|---|
| `fetch_news` | 获取新闻资讯 | keywords, date_range, max_count |
| `search_web` | 网页搜索 | query, num_results |
| `fetch_weather` | 天气查询 | city, date |
| `send_email` | 发送邮件（模拟） | to, subject, body |
| `get_stock_price` | 股票行情查询 | symbol, date_range |

详见 [MOCK_API.md](./MOCK_API.md)。

---

## 5. 功能清单

### 5.1 会话管理

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| S-01 | 自动创建会话 | P0 | 首次访问自动分配 Session ID |
| S-02 | 会话持久保留 | P0 | 长期有效，刷新不丢失 |
| S-03 | 会话恢复 | P1 | 重新访问恢复到上次状态 |

### 5.2 智能体创建

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| C-01 | NL 输入框 | P0 | 自由文本描述需求 |
| C-02 | LLM 动态反问 | P0 | 信息不足时 LLM 自动追问 |
| C-03 | 自动类型推断 | P0 | 无需用户选择类型（普通 / 工具调用） |
| C-04 | System Prompt 自动生成 | P0 | LLM 根据描述生成完整 prompt |
| C-05 | 工具自动推断 | P0 | 工具调用型：LLM 自主选择需要哪些工具 |
| C-06 | 创建成功通知 | P0 | 提示用户进入测试 |

### 5.3 测试对话

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| T-01 | 多轮对话 | P0 | 保持上下文的连续聊天 |
| T-02 | SSE 流式输出 | P0 | 逐 Token 展示回答 |
| T-03 | 工具调用日志展示 | P0 | 工具调用型：展示调用的工具、参数、结果 |
| T-04 | 可用工具列表展示 | P1 | 工具调用型：页面顶部展示可用的工具名称 |

### 5.4 反馈迭代

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F-01 | 对话式反馈 | P0 | 自然语言批评或建议 |
| F-02 | Prompt 自动调优 | P0 | LLM 根据反馈重写 system prompt |
| F-03 | 工具列表调优 | P1 | 工具调用型：可增减工具 |
| F-04 | 即时生效 | P0 | 调优后立即可测试 |
| F-05 | 满意确认 | P1 | 用户标记当前版本可接受 |

### 5.5 智能体管理

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| M-01 | 智能体列表 | P0 | 展示会话内所有智能体 |
| M-02 | 智能体删除 | P0 | 删除不需要的智能体 |
| M-03 | 重命名 | P1 | 修改智能体名称 |

---

## 6. 非功能需求

### 6.1 用户体验

- 所有技术术语对用户不可见
- 流式输出提供即时反馈感
- 工具调用日志用非技术语言展示（"正在查询天气…"而非 "POST /api/actions/fetch_weather"）
- 从输入到创建 ≤ 1 分钟

### 6.2 数据与隐私

- **用户身份隔离**：提供极简风格的内网账号注册与登录系统，每个用户拥有独立的账户标识。
- **数据专属与多端同步**：所有对话、会话以及创建的智能体数据基于 `userId` 在后端（MySQL）进行物理隔离，支持用户换端登录后无缝拉取自己创建的所有内容。
- **持久化保留**：通过 MySQL 数据库实现数据的永久稳定保留。

### 6.3 扩展性预留

- 智能体类型可扩展
- 工具列表可扩展（新增功能性 API）
- 数据架构已从 SQLite 平滑升级至 MySQL，保障多用户并发吞吐性能。

---

## 7. 产品实现状态与范围清单

- **已完成核心特性**：
  - **极简登录与注册系统**：前端提供单入口（Tab 切换）的黑调毛玻璃登录卡片，后端使用 Casdoor ROPC 密码授权模式，直接支持纯局域网内的“用户名+密码”认证，剔除了所有 Casdoor 官方标识与外链。
  - **数据库迁移**：存储系统整体迁移至虚拟机 MySQL，数据模型针对各字段显式限制了长度约束以保证高并发吞吐。
  - **对话与生成页面对齐优化**：对话测试页 (`test.html`) 和主页 (`index.html`) 的聊天区域及输入框在任意分辨率下均完全水平居中对称，并增加了主页输入框垂直悬浮与平滑下滑等动效。
- **不做清单（后续版本范围外）**：
  - ❌ RAG 知识库 / 文档上传
  - ❌ Agent 市场 / 分享
  - ❌ 定时执行智能体（当前版本为"对话中触发工具调用"）
  - ❌ 真实外部 API 计费系统
  - ❌ 嵌入第三方平台
  - ❌ Agent 版本管理

---

## 8. 成功标准

1. 用户从输入需求到创建智能体 ≤ 3 轮对话
2. 用户无需了解任何技术概念即可完成全部操作
3. 反馈调优后智能体行为明显改善（用户可感知）
4. 工具调用型智能体能自主判断何时调用、调用哪个工具
5. 会话数据长期不丢失（Session 永久保留）
